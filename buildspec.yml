version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10  
  build:
    commands:
      - echo Nothing to do yet
artifacts:
  files:
    - index.html
    - favicon.ico
    - RobinNorwood-Resume.pdf
    - styles/main.css
    - images/*
